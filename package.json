{
  "name": "api-nodejs-clean-arch-tdd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "test": "jest --watch --silent --colors --noStackTrace",
    "test:staged": "jest --passWithNoTests --findRelatedTests --silent --colors --noStackTrace",
    "test:ci": "jest --coverage --silent --colors --noStackTrace"
  },
  "standard": {
    "env": ["jest"]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3",
    "jest": "^29.6.2",
    "lint-staged": "^14.0.0",
    "standard": "^17.1.0"
  }
}
